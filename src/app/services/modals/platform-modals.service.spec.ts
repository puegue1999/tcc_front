import { TestBed } from '@angular/core/testing';

import { PlatformModalsService } from './platform-modals.service';

describe('PlatformModalsService', () => {
  let service: PlatformModalsService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(PlatformModalsService);
  });

});
